<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="appName" source="spring.application.name"/>
    <springProperty scope="context" name="kafkaBootstrapServers" source="logging.kafka.bootstrap-servers" defaultValue="localhost:9091,localhost:9092,localhost:9093"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <springProfile name="kafka-logging">
        <appender name="KAFKA" class="com.lhf.hotel.common.logging.KafkaLogbackAppender">
            <topic>logs-topic</topic>
            <bootstrapServers>${kafkaBootstrapServers}</bootstrapServers>
            <appName>${appName}</appName>
        </appender>
        <root level="INFO">
            <appender-ref ref="KAFKA"/>
        </root>
        <logger name="org.apache.kafka" level="WARN"/>
    </springProfile>
</configuration>
